jQuery(function() {
    var gallery = jQuery('.featured-products-swiper-container').swiper({
        slidesPerView : 'auto',
        resizeReInit:true,
        keyboardControl:true,
        grabCursor:true
    });
});